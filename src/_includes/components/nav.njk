<!-- Navigation -->
<nav class="bg-white shadow-md fixed w-full top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-20">
            <!-- Logo -->
            <div class="flex items-center gap-3">
                <img src="{{ imagePath }}images/logo.png" alt="Logo" class="w-10 h-10 object-contain">
                <div>
                    <div class="font-playfair text-navy text-lg leading-tight">Rocío Touza Sánchez</div>
                    <div class="text-sm text-gray-600">{{ t.footer.tagline }}</div>
                </div>
            </div>

            <!-- Desktop Menu -->
            <div class="hidden md:flex items-center gap-8">
                <a href="#inicio" class="nav-link text-navy font-medium">{{ t.nav.home }}</a>
                <a href="#sobre-mi" class="nav-link text-navy font-medium">{{ t.nav.about }}</a>
                <a href="#servicios" class="nav-link text-navy font-medium">{{ t.nav.services }}</a>
                <a href="#modalidades" class="nav-link text-navy font-medium">{{ t.nav.modalities }}</a>
                <a href="#faq" class="nav-link text-navy font-medium">{{ t.nav.faq }}</a>
                <a href="#reservar" class="nav-link text-navy font-medium">{{ t.nav.book }}</a>

                <!-- Language Switcher -->
                <a href="{{ altLangUrl }}" class="px-4 py-2 bg-cream rounded-full font-medium hover:bg-opacity-90 transition inline-block">
                    <span class="{% if lang == 'es' %}font-bold text-forest-green{% else %}text-gray-400{% endif %}">ES</span>
                    <span class="text-gray-400 mx-2">|</span>
                    <span class="{% if lang == 'en' %}font-bold text-forest-green{% else %}text-gray-400{% endif %}">EN</span>
                </a>
            </div>

            <!-- Mobile Menu Button -->
            <button id="mobileMenuBtn" class="md:hidden p-2" aria-label="{{ t.nav.ariaMenuLabel }}" aria-expanded="false">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="hidden md:hidden bg-white border-t">
        <div class="px-4 py-4 space-y-3">
            <a href="#inicio" class="block py-2 text-navy">{{ t.nav.home }}</a>
            <a href="#sobre-mi" class="block py-2 text-navy">{{ t.nav.about }}</a>
            <a href="#servicios" class="block py-2 text-navy">{{ t.nav.services }}</a>
            <a href="#modalidades" class="block py-2 text-navy">{{ t.nav.modalities }}</a>
            <a href="#faq" class="block py-2 text-navy">{{ t.nav.faq }}</a>
            <a href="#reservar" class="block py-2 text-navy">{{ t.nav.book }}</a>

            <!-- Language Switcher for Mobile -->
            <div class="mt-4 flex justify-center">
                <a href="{{ altLangUrl }}" class="px-6 py-2 bg-cream rounded-full font-medium hover:bg-opacity-90 transition inline-block">
                    <span class="{% if lang == 'es' %}font-bold text-forest-green{% else %}text-gray-400{% endif %}">ES</span>
                    <span class="text-gray-400 mx-2">|</span>
                    <span class="{% if lang == 'en' %}font-bold text-forest-green{% else %}text-gray-400{% endif %}">EN</span>
                </a>
            </div>
        </div>
    </div>
</nav>
